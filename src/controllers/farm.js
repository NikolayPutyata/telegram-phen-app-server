// import { startFarming } from '../services/farm';

// export const farmStartController = async (req, res) => {
//   const price = req.body.price;
//   const id = req.body.id;

//   const data = await startFarming(id, price);

//   res.status(200).json({ isFarmingStart: false });
// };
// export const farmUpdateController = async (req, res) => {
//   const price = req.body.price;
//     const id = req.body.id;

//     res.status(200).json({});
// };
